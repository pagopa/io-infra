<!--
    api_auth.yaml base policy
-->
<policies>
    <inbound>
        <!-- Skip product policy -->
        <!-- <base /> -->
        
        <!-- TODO move set-backend-service at product level, now we have a dependency on final path with /api/v1, so we need to set this param here -->
        <set-backend-service base-url="https://io-p-app-appbackendl1.azurewebsites.net" />
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>
